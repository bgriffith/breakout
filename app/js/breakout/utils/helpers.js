let helpers = {
  /**
   * Convert degrees to radians
   * @param {int} degrees - The value in degrees to convert
   * @returns {int} Degrees value in radians
   */
  degToRad: (degrees) => {
    return degrees * (Math.PI / 180);
  }
};

export default helpers;
